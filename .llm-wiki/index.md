# Project LLM Wiki Index

Start here before using other `.llm-wiki/` pages.

- Summary seed: [[summaries/repo-overview]]
- Durable ideas: [[features/ideas]]

## Architecture

Use `architecture/` for stable system boundaries, data flows, and tradeoffs.

## Domain

Use `domain/` for project vocabulary, actors, concepts, and domain rules.

## Decisions

Use `decisions/` for durable decision records and rejected alternatives.

## Operations

Use `operations/` for runbooks, maintenance notes, and recurring procedures.

## Features

Use `features/` for validated feature summaries and durable future ideas.

## Summaries

Use `summaries/` for concise, provenance-backed project overviews.

## Raw Sources

Use `raw/curated/` only for small, de-secreted, intentionally selected sources or excerpts. See [[raw/README]] and [[raw/curated/README]] before adding material.
